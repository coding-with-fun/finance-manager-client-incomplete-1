/**
 * @author Harsh Patel @harsh2124
 */

const initialState = {
    loading: false,
    isAuthenticated: true,
    error: '',
};

const user = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default user;
