import React from 'react';
import { Col } from 'react-bootstrap';

const Expense = () => {
    return (
        <Col sm={6}>
            <h1>Expense</h1>
        </Col>
    );
};

export default Expense;
